{
  "mcpServers": {
    "postgresql": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-postgres",
        "postgresql://peterlim@localhost:5432/event_cms_dev"
      ]
    },
    "ssh-mcp": {
      "command": "npx",
      "args": [
        "ssh-mcp",
        "-y",
        "--",
        "--host=20.127.186.107",
        "--port=22",
        "--user=azureuser",        
        "--key=/home/peterlim/.ssh/digital-church-key.pem",
        "--timeout=30000"
      ]
    }
  }
}